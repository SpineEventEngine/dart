name: client_test
description: Tests for package `spine_client`.
publish_to: none

environment:
  sdk: '>=2.13.0 <3.0.0'

dependencies:
  spine_client:
    path: ../../client
  firebase_dart: ^1.0.9

dev_dependencies:
  pedantic: ^1.11.0
  test: ^1.17.4
